{
    "name": "military-asset-manager-root",
    "version": "1.0.0",
    "description": "Root configuration for Monorepo deployment",
    "scripts": {
        "build": "npm install --prefix client && npm run build --prefix client"
    },
    "dependencies": {
        "alasql": "^4.0.0",
        "body-parser": "^1.20.2",
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "serverless-http": "^3.2.0"
    }
}